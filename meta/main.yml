galaxy_info:
  author: Ofer Sadan
  description: Install the rclone docker plugin
  company: Zero-ML
  license: MIT
  min_ansible_version: "2.9"
  galaxy_tags: [docker, rclone]
  platforms:
    - name: Ubuntu
      versions:
        - focal

dependencies:
  - role: community.docker.docker_plugin
